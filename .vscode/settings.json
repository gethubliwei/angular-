// 将设置放入此文件中以覆盖默认值和用户设置。
{   //是否使用background插件
    "background.enabled": true,
    //是否使用默认图，当为false时使用下面customImages中的地址
    "background.useDefault": false,
    //选择最多三个图片地址
    "background.customImages": [
        
    ]
}